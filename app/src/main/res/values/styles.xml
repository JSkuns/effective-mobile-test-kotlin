<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Стиль для кнопки VK -->
    <style name="Button.VK" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/vk_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">28dp</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <!-- Стиль для кнопки OK -->
    <style name="Button.OK" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/ok_orange</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">28dp</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <!-- Стиль для основной кнопки -->
    <style name="Button.Primary" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">28dp</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <style name="Widget.App.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemBackground">@drawable/bottom_nav_background_color</item>
        <item name="itemIconTint">@color/bottom_nav_color</item>
        <item name="itemTextColor">@color/bottom_nav_color</item>
    </style>
</resources>